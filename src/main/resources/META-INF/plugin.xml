<!-- Plugin Configuration File. Read more: https://plugins.jetbrains.com/docs/intellij/plugin-configuration-file.html -->
<idea-plugin>
    <id>dev.gaphunter.rubynethttpreusecompanion</id>
    <name>Ruby Net::HTTP Reuse Companion</name>
    <vendor>Gap Hunter Labs</vendor>
    <description><![CDATA[
        <p>Warning on a <code>Net::HTTP.get</code>/
        <code>.get_response</code>/<code>.post</code> shorthand call
        found inside a loop/iterator block (<code>.each do</code>,
        <code>.times do</code>, <code>while</code>, <code>for</code>,
        brace-block <code>{ }</code>) -- Ruby's own <code>Net::HTTP</code>
        documentation explains why this matters: "Creating a new HTTP
        connection for every request involves an extra TCP round-trip
        and causes TCP congestion avoidance negotiation to start
        over". The shorthand class methods open and close a connection
        on every single call, so using them inside a loop means paying
        that cost on every iteration instead of once via
        <code>Net::HTTP.start(...)</code>.</p>
        <p><b>100% static text analysis</b> -- a `do`/`end`/brace
        nesting-depth line scanner, not a real Ruby parser, so it works
        whether the Ruby plugin is installed or not.</p>
        <p>Stated honestly: v0.1 only recognizes the common
        <code>.each do</code>/<code>.times do</code>/<code>while</code>/
        <code>for ... do</code>/brace-block loop-header shapes -- a loop
        built some other way (recursion, an unusual
        <code>loop { }</code> form) isn't specially tracked. Matches by
        simple text, not real call resolution -- an unrelated
        <code>.get</code>/<code>.post</code> method on some other
        object is a possible (rare) false positive.</p>
        <p>Free. No license nags, no telemetry, no network calls.</p>
    ]]></description>

    <depends>com.intellij.modules.platform</depends>

    <extensions defaultExtensionNs="com.intellij">
        <localInspection language=""
            groupName="Ruby Net::HTTP Reuse Companion"
            displayName="Net::HTTP connection opened per loop iteration"
            enabledByDefault="true"
            level="WARNING"
            implementationClass="dev.gaphunter.rubynethttpreusecompanion.inspection.NetHttpConnectionPerIterationInspection"/>
        <notificationGroup id="Ruby Net::HTTP Reuse Companion" displayType="BALLOON"/>
    </extensions>
</idea-plugin>
